---
"@open-pioneer/check-pnpm-duplicates": minor
---

Add `--update` flag to automatically update the config file's `allowed` list with current duplicates.

Usage: `check-pnpm-duplicates -c path/to/config.yaml --update`
