---
"@open-pioneer/build-package": minor
"@open-pioneer/build-package-cli": minor
---

Copy files configured as `publishConfig.licenseFile` and `publishConfig.noticeFile` to the package's `dist` directory.
