---
"@open-pioneer/build-support": minor
---

Add the `publishConfig.packageFormatTarget` to customize the target (trails-) package version. Raising this value enables new features but also needs an updating vite plugin on the package consumer's side.
